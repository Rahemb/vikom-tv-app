<?xml version="1.0" encoding="utf-8"?>
<!-- Status badge. Pill radius, matching the web portal where .badge uses the
     pill token while buttons use the small radius. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/vk_gray_600" />
    <corners android:radius="@dimen/radius_pill" />
</shape>
