<?xml version="1.0" encoding="utf-8"?>
<!--
  Home.

  The next visit owns the top of the screen because it is the reason the tablet is
  in the room; the three tiles below are the only other places the app goes. There
  is no tab strip: every destination is one press from here, and Back always
  returns here, so a patient cannot get lost.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/padding_large"
    android:paddingTop="4dp"
    android:paddingBottom="@dimen/padding_standard">

    <!-- Next visit -->
    <LinearLayout
        android:id="@+id/hero_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/appointment_card_background"
        android:clickable="true"
        android:defaultFocusHighlightEnabled="false"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusDown="@+id/tile_appointments"
        android:orientation="vertical"
        android:padding="@dimen/padding_standard">

        <TextView
            android:id="@+id/txt_hero_eyebrow"
            style="@style/VK.Text.Body.Emphasis"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:letterSpacing="0.12"
            android:textAllCaps="true"
            android:textColor="@color/vk_primary" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/txt_hero_time"
                    style="@style/VK.Text.Title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:includeFontPadding="false"
                    android:textSize="@dimen/hero_time_size" />

                <TextView
                    android:id="@+id/txt_hero_who"
                    style="@style/VK.Text.Heading"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="2" />
            </LinearLayout>

            <TextView
                android:id="@+id/txt_hero_status"
                style="@style/VK.Text.Caption"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/status_chip_booked"
                android:paddingHorizontal="@dimen/padding_standard"
                android:paddingVertical="8dp"
                android:textColor="@color/vk_white"
                android:textStyle="bold" />
        </LinearLayout>

        <!--
          Task badges get the full width of the card rather than a column beside
          the status chip: three of them never fit in that column, and a patient
          reads "what is being done" as one line, not a stack.
        -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/padding_standard"
            android:scrollbars="none">

            <LinearLayout
                android:id="@+id/hero_tasks"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal" />
        </HorizontalScrollView>

        <!-- Loading / empty / error all speak through this one line -->
        <TextView
            android:id="@+id/txt_hero_state"
            style="@style/VK.Text.Body.Muted"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingVertical="@dimen/padding_large"
            android:visibility="gone" />
    </LinearLayout>

    <Space
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- The three places this app goes -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/padding_standard"
        android:orientation="horizontal">

        <LinearLayout
            android:id="@+id/tile_appointments"
            style="@style/VK.HomeTile"
            android:nextFocusUp="@id/hero_card"
            android:nextFocusRight="@+id/tile_contacts">

            <ImageView
                style="@style/VK.HomeTile.Icon"
                android:src="@drawable/ic_calendar" />

            <TextView
                style="@style/VK.HomeTile.Label"
                android:text="@string/home_all_appointments" />

            <TextView
                android:id="@+id/txt_tile_appointments_sub"
                style="@style/VK.HomeTile.Sub" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/tile_contacts"
            style="@style/VK.HomeTile"
            android:layout_marginStart="@dimen/padding_standard"
            android:nextFocusUp="@id/hero_card"
            android:nextFocusLeft="@id/tile_appointments"
            android:nextFocusRight="@+id/tile_care_team">

            <ImageView
                style="@style/VK.HomeTile.Icon"
                android:src="@drawable/ic_call" />

            <TextView
                style="@style/VK.HomeTile.Label"
                android:text="@string/home_call_someone" />

            <TextView
                android:id="@+id/txt_tile_contacts_sub"
                style="@style/VK.HomeTile.Sub" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/tile_care_team"
            style="@style/VK.HomeTile"
            android:layout_marginStart="@dimen/padding_standard"
            android:nextFocusUp="@id/hero_card"
            android:nextFocusLeft="@id/tile_contacts">

            <ImageView
                style="@style/VK.HomeTile.Icon"
                android:src="@drawable/ic_care_team" />

            <TextView
                style="@style/VK.HomeTile.Label"
                android:text="@string/home_care_team" />

            <TextView
                android:id="@+id/txt_tile_care_team_sub"
                style="@style/VK.HomeTile.Sub" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
