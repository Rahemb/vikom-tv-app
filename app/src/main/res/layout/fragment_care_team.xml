<?xml version="1.0" encoding="utf-8"?>
<!--
  The patient's care team: who looks after them, and the phone number to reach
  them on.

  Read-only on purpose. Personnel authenticate against the portal, not Supabase,
  so they have no profile the tablet could place a call to — showing a number the
  patient can dial on their own phone is honest; a Ring button that failed would
  not be.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:orientation="vertical"
    android:paddingHorizontal="@dimen/padding_large"
    android:paddingTop="4dp"
    android:paddingBottom="@dimen/padding_standard">

    <include layout="@layout/view_screen_header" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@drawable/content_area_background"
        android:orientation="vertical"
        android:padding="8dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/care_team_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingHorizontal="4dp" />

            <TextView
                android:id="@+id/txt_state"
                style="@style/VK.Text.Body.Muted"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:paddingHorizontal="@dimen/padding_large" />
        </FrameLayout>
    </LinearLayout>
</LinearLayout>
